import { FilePointer, Pointer } from './parseSchool';

export class ParseSection {
    objectId: string;
    name: string;
    school: Pointer;
    createdAt: string;
    updatedAt: string;
    image: FilePointer;
}

